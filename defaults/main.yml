---
# defaults file for roles/letsencrypt
# web_server: httpd
# document_root: '/var/www/html'
# web_server: nginx
letsencrypt_web_server: httpd
letsencrypt_document_root: '/var/www/html/project/fw/laravel9/public'
letsencrypt_fqdn: "{{ secret.letsencrypt.fqdn }}"
letsencrypt_email: "{{ secret.letsencrypt.email }}"
